name: apex_lsp
description: Apex Language Server Protocol (LSP) server over stdio.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0

executables:
  apex_lsp: apex_lsp
